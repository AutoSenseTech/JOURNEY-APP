//  Stepper.swift
//  journayDemo
//
//  Created by MU IT Program on 5/3/16.
//  Copyright © 2016 Wang Weihan. All rights reserved.
//

import Foundation
import CoreData
import UIKit

class Stepper {

    static func addStepsAndTime() {
        /*
        var thisTrail: NSManagedObject?
        let appDelegate = UIApplication.sharedApplication().delegate as! AppDelegate
        let managedContext = appDelegate.managedObjectContext
        let trailEntity =  NSEntityDescription.entityForName("Trail", inManagedObjectContext:   managedContext)
        thisTrail = NSManagedObject(entity: trailEntity!, insertIntoManagedObjectContext:managedContext)
        thisTrail?.setValue(Int(arc4random_uniform(12) + 1), forKey: "totalMiles")
        thisTrail?.setValue(Int(arc4random_uniform(1) + 2), forKey: "timeSpent")
        
        do {
            try managedContext.save()
        } catch let error as NSError {
            print("Could not save \(error), \(error.userInfo)")
        }*/

        //miles and days
    }
}
